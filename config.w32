// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("skynet", "for skynet support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("skynet", "enable skynet support", "no");

if (PHP_SKYNET != "no") {
	EXTENSION("skynet", "skynet.c");
}

